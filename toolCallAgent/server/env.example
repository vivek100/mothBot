# =============================================================================
# Diagnostic Agent Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your API keys
# cp env.example .env

# =============================================================================
# OpenAI Configuration (Required for LLM)
# =============================================================================
OPENAI_API_KEY=your_openai_api_key_here

# Model options (use cheaper models for cost savings):
# - gpt-4o-mini (recommended - ~10x cheaper than gpt-4o)
# - gpt-3.5-turbo (cheapest option)
# - gpt-4o (most capable, more expensive)
OPENAI_MODEL=gpt-4o-mini

# =============================================================================
# Google Cloud Configuration (Required for Speech-to-Text and Text-to-Speech)
# =============================================================================
# Path to your Google Cloud service account JSON file
# Can be an absolute path or relative path from the server directory
# Example: ../credentials/your-credentials.json
GOOGLE_APPLICATION_CREDENTIALS=../../credentials/chalcio-40f65c2b8a6a.json
GOOGLE_LOCATION=us-central1

# Google Text-to-Speech Configuration
# Voice name options (valid voices):
# - en-US-Neural2-D (male, recommended)
# - en-US-Neural2-F (female)
# - en-US-Standard-B (male)
# - en-US-Standard-C (female)
# - en-US-Wavenet-D (male)
# - en-US-Wavenet-F (female)
# See: https://cloud.google.com/text-to-speech/docs/voices
# NOTE: Do NOT use Chirp3 voices (e.g., en-US-Chirp3-HD-Charon) - they are not available
GOOGLE_VOICE_NAME=en-US-Neural2-D
GOOGLE_LANGUAGE_CODE=en-US

# =============================================================================
# Optional: Daily.co Configuration (for Daily transport)
# =============================================================================
# DAILY_API_KEY=your_daily_api_key_here

# =============================================================================
# Optional: Pipecat Cloud Configuration
# =============================================================================
# PIPECAT_CLOUD_API_KEY=your_pipecat_cloud_key_here

# =============================================================================
# Skills API Configuration
# =============================================================================
# Starts a lightweight Skills API server alongside the bot process
# Default URL: http://localhost:7861
SKILLS_API_ENABLED=true
SKILLS_API_HOST=0.0.0.0
SKILLS_API_PORT=7861

# =============================================================================
# Weave (Weights & Biases) Tracing Configuration
# =============================================================================
# Your Weights & Biases API key for authentication
# Get it from: https://wandb.ai/authorize
WANDB_API_KEY=your_wandb_api_key_here

# Weave project name - traces will appear under this project in W&B
# Format: entity/project or just project (uses default entity)
WEAVE_PROJECT_NAME=toolCallAgent

# Optional: Set to "true" to disable tracing (useful for local dev without W&B)
# WEAVE_DISABLED=false

# Optional: Explicit thread ID for Weave thread grouping (useful for debugging)
# If not set, a unique session ID is generated per bot run.
# WEAVE_THREAD_ID=session_123

# =============================================================================
# Tool Delay Configuration (for testing)
# =============================================================================
# Diagnostic tools have simulated delays to mimic real sensor operations.
# Configure these to speed up testing or disable entirely.

# Set to "true" to disable all tool delays (instant execution)
# TOOL_DELAYS_DISABLED=false

# Minimum delay in seconds (default: 1.0)
# TOOL_DELAY_MIN=1.0

# Maximum delay in seconds (default: 3.0)
# TOOL_DELAY_MAX=3.0
